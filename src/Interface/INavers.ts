/* eslint-disable camelcase */
export interface INaver {
  admission_date: string;
  birthdate: string;
  id: string;
  name: string;
  job_role: string;
  project: string;
  url: string;
  user_id: string;
}
